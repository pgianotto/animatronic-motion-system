#!/bin/bash
# fpp-servo-calibrator installer
# Pure PHP/JS frontend — no daemon, no Python dependencies
set -e

echo "Installing Animatronic Servo Calibrator plugin..."
echo "No additional dependencies required."
echo "Access via FPP menu: Plugins > Servo Calibrator"
